rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // ESTO PERMITE TODO A CUALQUIERA AUTENTICADO
      allow read, write: if request.auth != null;
    }
  }
}